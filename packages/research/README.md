# Research package

Shared, dependency-free TypeScript helpers for the Phase 2 research layer.

The package intentionally remains small and deterministic so the static GitHub Pages application can reuse the same normalization and stable-ID rules as future server-side indexing services.
